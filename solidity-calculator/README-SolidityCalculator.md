# 🧮 Solidity SOLID Calculator

이 프로젝트는 단순한 문자열 덧셈 계산기를 구현한 것이지만, Solidity 개발 환경에서 객체지향 설계 원칙(SOLID)과 계층형 아키텍처(Layered Architecture)를 실험적으로 적용해본 결과물입니다.

단순한 기능을 수행하기 위해 여러 컨트랙트로 역할을 분리하는 **'의도된 오버 엔지니어링(Over-engineering)'**을 통해, 유지보수성과 확장성 있는 스마트 컨트랙트 구조를 연구했습니다.

## 🎯 프로젝트 목표
- 단일 책임 원칙(SRP) 준수: 계산 로직, 데이터 저장, 데이터 파싱의 책임을 분리합니다.
- 의존성 역전 원칙(DIP) 적용: 구체적인 컨트랙트(Adder, ResultStorage)가 아닌 인터페이스(IAdder, IResultStorage)에 의존하여 결합도를 낮춥니다.- 유지보수성: 비즈니스 로직이 변경되더라도 데이터 저장소(Storage)는 영향을 받지 않도록 설계합니다.

## 🏗️ 아키텍처 및 설계 (Architecture)
이 프로젝트는 **Controller(Calculator) - Service(Adder) - Repository(ResultStorage)**의 계층 구조와 유사하게 설계되었습니다.

주요 컴포넌트 역할컴포넌트유형역할 (Role)CalculatorContractOrchestrator. 사용자 입력을 받아 파싱하고, 덧셈을 위임하며, 결과를 저장소에 전달합니다.AdderContractPure Logic. 순수하게 숫자 배열을 받아 합계를 계산하는 역할만 수행합니다. 상태를 가지지 않습니다.ResultStorageContractState Management. 계산된 결과값(State)을 영구적으로 저장하고 조회합니다.StringSplitterLibraryUtility. 문자열 파싱, 커스텀 구분자 추출 등의 복잡한 문자열 처리 로직을 담당합니다

## 🚀 주요 기능 (Features)
- 문자열 덧셈 연산쉼표(,) 또는 콜론(;)을 기본 구분자로 사용하여 숫자를 더합니다.예: "1,2,3" => 6
- 커스텀 구분자 지원//와 \n 사이에 위치한 문자를 구분자로 사용할 수 있습니다.예: "//;\n1;2;3" => 6
- 결과값 온체인 저장연산 결과를 ResultStorage 컨트랙트에 저장하여 관리합니다.예외 처리음수 입력 방지 (Solidity uint 특성 활용 및 파싱 로직 검증)
- 잘못된 형식의 커스텀 구분자 입력 시 revert숫자 이외의 문자 포함 시 revert

## 🛠️ 기술 스택 (Tech Stack)
Language: Solidity ^0.8.0Framework: HardhatTesting: Hardhat Test (Mocha/Chai)

## 📂 파일 구조Bashcontracts/
├── Calculator.sol        # 메인 계산기 로직 (진입점)
├── Adder.sol             # 덧셈 연산 전용 컨트랙트
├── ResultStorage.sol     # 상태 저장소 컨트랙트
├── interfaces/           # 인터페이스 모음 (DIP 구현)
│   ├── IAdder.sol
│   ├── ICalculator.sol
│   └── IResultStorage.sol
└── libraries/            # 유틸리티 라이브러리
    └── StringSplitter.sol

## 💻 설치 및 실행 (Usage)
1. 의존성 설치 hnpm install
2. 컴파일 npx hardhat compile
3. 테스트 실행 npx hardhat test